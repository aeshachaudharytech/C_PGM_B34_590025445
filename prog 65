Q65: Search in a sorted array using binary search.

/*
Sample Test Cases:
Input 1:
5
1 3 5 7 9
7
Output 1:
Found at index 3

Input 2:
5
1 3 5 7 9
6
Output 2:
-1

*/

#include <stdio.h>

int main() {
    int n, key;
    scanf("%d", &n);

    int a[n];
    for(int i = 0; i < n; i++)
        scanf("%d", &a[i]);

    scanf("%d", &key);

    int l = 0, r = n - 1, mid, pos = -1;

    while(l <= r) {
        mid = (l + r) / 2;
        if(a[mid] == key) {
            pos = mid;
            break;
        } else if(a[mid] < key) {
            l = mid + 1;
        } else {
            r = mid - 1;
        }
    }

    if(pos == -1)
        printf("-1");
    else
        printf("Found at index %d", pos);

    return 0;
}
