Q40: Write a program to find the 1’s complement of a binary number and print it.

/*
Sample Test Cases:
Input 1:
1010
Output 1:
0101

Input 2:
1111
Output 2:
0000

*/

#include <stdio.h>
#include <string.h>

int main() {
    char binary[100], onesComplement[100];

    // Input binary number as a string
    scanf("%s", binary);

    int length = strlen(binary);

    // Process each bit
    for (int i = 0; i < length; i++) {
        if (binary[i] == '0') {
            onesComplement[i] = '1';
        } else if (binary[i] == '1') {
            onesComplement[i] = '0';
        } else {
            // Invalid input if not 0 or 1
            printf("Invalid input\n");
            return 1;
        }
    }

    onesComplement[length] = '\0'; // Null-terminate the result string

    // Output the 1’s complement
    printf("%s\n", onesComplement);

    return 0;
}
