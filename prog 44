Q44: Write a program to find the sum of the series: 1 + 3/4 + 5/6 + 7/8 + … up to n terms.

/*
Sample Test Cases:
Input 1:
3
Output 1:
Approximate sum: 3.3

Input 2:
5
Output 2:
Approximate sum: 4.4

*/

#include <stdio.h>

int main() {
    int n;
    double sum = 0.0;

    // Input number of terms
    scanf("%d", &n);

    int numerator = 1;
    int denominator = 1;

    // Loop to calculate sum up to n terms
    for (int i = 1; i <= n; i++) {
        denominator = numerator + 1; // denominator is always one more than numerator
        sum += (double)numerator / denominator;
        numerator += 2; // next odd number
    }

    // Output the result with one decimal place as approximate sum
    printf("Approximate sum: %.1f\n", sum);

    return 0;
}
