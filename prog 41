Q41: Write a program to swap the first and last digit of a number.

/*
Sample Test Cases:
Input 1:
1234
Output 1:
4231

Input 2:
1001
Output 2:
1001

*/

#include <stdio.h>
#include <math.h>

int main() {
    int number, first, last, digits = 0, temp, result;

    // Input the number
    scanf("%d", &number);

    temp = number;
    last = temp % 10; // Get the last digit

    // Count digits and get the first digit
    while (temp >= 10) {
        temp = temp / 10;
        digits++;
    }
    first = temp; // Now temp is the first digit

    // If number has only one digit or first and last are same, no change needed
    if (digits == 0 || first == last) {
        printf("%d\n", number);
        return 0;
    }

    int power = pow(10, digits);      // 10^(number of digits)
    result = last * power +          // Put last digit in first place
             (number % power / 10) * 10 + // Middle part stays same
             first;                   // Put first digit at last position

    printf("%d\n", result);

    return 0;
}
