#include <stdio.h>

int main() {
    char str[1000];
    int count = 0;
    fgets(str, sizeof(str), stdin);

    while(str[count] != '\n' && str[count] != '\0') {
        count++;
    }

    printf("%d", count);
    return 0;
}
